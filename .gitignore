.venv/
__pycache__/
*.py[cod]
.pytest_cache/
output.mp4

# Médias de test (gros binaires, jamais commités)
tracks/
clips/
output/

# File de publication, plan et réglages réels (données locales)
queue/
plan.toml
settings.json

# Secrets TikTok et tokens OAuth — JAMAIS dans git
.env
tokens/
tiktok*.txt
.DS_Store
.superpowers/

# Base de données plateforme et données locales
platform.db
data/
out.mp4

# Front PoC (React/Vite) — dépendances et build
frontend/node_modules/
frontend/dist/

# Fichier de travail perso (liens de clips à importer) — jamais suivi,
# contrairement à links.txt qui l'est depuis le début du projet.
clip_links.txt

# Captures d'écran déposées à la racine (partage de bugs, revues visuelles).
# Deux d'entre elles se sont retrouvées commitées par accident : 1,8 puis 2,9 Mo,
# avec apostrophe typographique et accents dans le nom — et GitHub refuse le push
# avec un « HTTP 400 » qui ne dit pas pourquoi. La règle vaut mieux que la
# vigilance.
Capture*.png
Screenshot*.png
